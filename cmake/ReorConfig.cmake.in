# Config file for Reor, providing imported targets
# for the package.
#
# After successful configuration the following variables
# will be defined:
#
# REOR_VERSION the version number of Reor

@PACKAGE_INIT@

set_and_check(REOR_INCLUDE_DIRS "@PACKAGE_REOR_INSTALL_INCLUDE_DIR@")

if(NOT TARGET Reor::reor AND NOT REOR_BINARY_DIR)
  include("@PACKAGE_REOR_INSTALL_CMAKE_DIR@/ReorTargets.cmake")
endif()
